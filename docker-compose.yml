version: '3.9'

services:
  app:
    build: .
    container_name: app
    volumes:
      - .:/app
    env_file:
    - .env    
    networks:
      - mongo-net
    deploy:
        resources:
            limits:
              cpus: '1'
              memory: 512M
            reservations:
              cpus: '0.5'
              memory: 128M

networks:
  mongo-net:
    external: True